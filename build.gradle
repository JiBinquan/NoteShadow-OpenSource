buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // Newest Android Gradle Plugin already present in the supplied workstation.
        classpath 'com.android.tools.build:gradle:7.4.1'
    }
}
